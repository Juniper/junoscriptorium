<?xml version="1.0"?>
<script>
  <title>run-on-all.slax</title>
  <author>phil.shafer</author>
  <synopsis>
	Run a command on multiple remote targets
  </synopsis>
  <coe>op</coe>
  <type>display</type>

  <description>
This script runs a command on a set of remote hosts.  The
output from all targets is displayed.

Note that this uses the "jcs:execute()" function, so the
user must set up ssh-agent and keys as needed to allow
unprompted login.

  </description>

<example>
  <title>Sample session</title>
  <config>example-1.conf</config>
  <output>example-1.output</output>
</example>

  <xhtml:script xmlns:xhtml="http://www.w3.org/1999/xhtml"
                src="../../../../../web/leaf.js" 
	        type="text/javascript"/>
</script>
